# Database
# For local: leave empty to use Supabase Local (postgresql://postgres:postgres@127.0.0.1:54322/northstar)
# For production: set to Supabase cloud connection string
# DATABASE_URL="postgresql://postgres.xxx:password@aws-0-sa-east-1.pooler.supabase.com:6543/postgres"

# Auth.js (NextAuth)
# Generate secret with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# OAuth Providers (optional - for signup via Google/GitHub)
# Google OAuth - Get from: https://console.cloud.google.com/apis/credentials
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# GitHub OAuth - Get from: https://github.com/settings/developers
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/github
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Cloudflare Turnstile (required for login captcha)
# For LOCAL DEV, use Cloudflare test keys (always passes):
#   NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
#   TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
#
# For PRODUCTION, get real keys from: https://dash.cloudflare.com/?to=/:account/turnstile
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_site_key
TURNSTILE_SECRET_KEY=your_secret_key

# Site URL (required for password reset email links)
# For local: http://localhost:3000
# For production: your deployed URL (e.g., https://yourapp.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email (Resend)
RESEND_API_KEY=re_xxxxxxxxx
RESEND_FROM_EMAIL=noreply@yourdomain.com

# Cron
CRON_SECRET=your-secret-here

# Rate Limiting
# For PRODUCTION: Use Upstash Redis (get from: https://upstash.com/)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# For LOCAL DEV: Use Docker Redis (default: redis://localhost:6379)
REDIS_URL=redis://localhost:6379

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Firebase (Push Notifications)
# Get from: https://console.firebase.google.com/project/YOUR_PROJECT/settings/cloudmessaging
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_VAPID_KEY=

# Firebase Admin Service Account
# Option 1: Base64-encoded JSON (RECOMMENDED - avoids escaping issues)
#   Generate with: base64 -i your-service-account.json | tr -d '\n'
#   Then set: FIREBASE_ADMIN_SERVICE_ACCOUNT_BASE64=eyJ0eXBlIjoi...
FIREBASE_ADMIN_SERVICE_ACCOUNT_BASE64=

# Option 2: Raw JSON (may have newline/escaping issues on some platforms)
#   Example: FIREBASE_ADMIN_SERVICE_ACCOUNT='{"type":"service_account",...}'
FIREBASE_ADMIN_SERVICE_ACCOUNT=

# E2E Testing (optional)
E2E_AUTH_BYPASS=false
E2E_AUTH_USER_ID=
